/**
 * 
 * higher order function are the function which contains function in its params or return  the function.
 * higher order function like map, filter , setTimeout, setTimeIn etc.
 */
higher_order(params);

function params() {
   return console.log('hi I am params');
}

function higher_order(params) {
    params();
}